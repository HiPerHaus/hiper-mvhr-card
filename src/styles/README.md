# src/styles

Shared CSS custom properties and theme tokens. Must respect Home Assistant's light/dark theme variables — no hardcoded colors outside status semantics (ok/warning/error). See `SPECIFICATION.md` §7.

No implementation yet — see `ROADMAP.md`.
