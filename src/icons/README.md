# src/icons

Custom SVG icons for MVHR concepts not covered by Home Assistant's MDI icon set (e.g. bypass state, heat recovery). See `docs/architecture.md` §2.

No implementation yet — Phase 1's card interface uses plain text/labels only, no custom icons.
