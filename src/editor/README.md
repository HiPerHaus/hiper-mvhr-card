# src/editor

The Lovelace visual config editor custom element (`hiper-mvhr-card-editor`), kept separate from `src/components` because it implements a different lifecycle/contract than the display card and is only loaded while a user is editing their dashboard. See `docs/architecture.md` §2 and §9.

No implementation yet — see `ROADMAP.md` Phase 5.
